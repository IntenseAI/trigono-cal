import { useState, useEffect } from 'react';

[{"path":"src/app/page.tsx","content":"'use client';\n\nimport { useState, useEffect } from 'react';\nimport SynoppyBadge from '@/components/SynoppyBadge';\n\ntype TrigFunction = 'sin' | 'cos' | 'tan' | 'csc' | 'sec' | 'cot';\ntype AngleMode = 'deg' | 'rad';\n\ninterface ConversionResult {\n  function: TrigFunction;\n  value: number;\n  formula: string;\n}\n\nexport default function CalculatorPage() {\n  const [angle, setAngle] = useState<string>('45');\n  const [selectedFunction, setSelectedFunction] = useState<TrigFunction>('sin');\n  const [angleMode, setAngleMode] = useState<AngleMode>('deg');\n  const [theme, setTheme] = useState<'light' | 'dark'>('light');\n  const [mounted, setMounted] = useState(false);\n\n  useEffect(() => {\n    setMounted(true);\n    if (typeof window !== 'undefined') {\n      const savedTheme = localStorage.getItem('theme') as 'light' | 'dark' | null;\n      if (savedTheme) {\n        setTheme(savedTheme);\n        document.documentElement.classList.toggle('dark', savedTheme === 'dark');\n      }\n    }\n  }, []);\n\n  useEffect(() => {\n    if (mounted && typeof window !== 'undefined') {\n      localStorage.setItem('theme', theme);\n      document.documentElement.classList.toggle('dark', theme === 'dark');\n    }\n  }, [theme, mounted]);\n\n  const toggleTheme = () => {\n    setTheme(prev => prev === 'light' ? 'dark' : 'light');\n  };\n\n  const toRadians = (deg: number): number => (deg * Math.PI) / 180;\n  const toDegrees = (rad: number): number => (rad * 180) / Math.PI;\n\n  const getAngleInRadians = (): number => {\n    const numAngle = parseFloat(angle) || 0;\n    return angleMode === 'deg' ? toRadians(numAngle) : numAngle;\n  };\n\n  const calculateTrigValue = (func: TrigFunction, radAngle: number): number => {\n    switch (func) {\n      case 'sin': return Math.sin(radAngle);\n      case 'cos': return Math.cos(radAngle);\n      case 'tan': return Math.tan(radAngle);\n      case 'csc': return 1 / Math.sin(radAngle);\n      case 'sec': return 1 / Math.cos(radAngle);\n      case 'cot': return 1 / Math.tan(radAngle);\n      default: return 0;\n    }\n  };\n\n  const getConversions = (): ConversionResult[] => {\n    const radAngle = getAngleInRadians();\n    const numAngle = parseFloat(angle) || 0;\n    const displayAngle = angleMode === 'deg' ? `${numAngle}°` : `${numAngle} rad`;\n    const complementAngle = angleMode === 'deg' ? `${90 - numAngle}°` : `${Math.PI/2 - numAngle} rad`;\n\n    const conversions: ConversionResult[] = [];\n\n    switch (selectedFunction) {\n      case 'sin':\n        conversions.push(\n          { function: 'sin', value: Math.sin(radAngle), formula: `sin(${displayAngle})` },\n          { function: 'cos', value: Math.cos(Math.PI/2 - radAngle), formula: `cos(${complementAngle})` },\n          { function: 'csc', value: 1 / Math.sin(radAngle), formula: `1/sin(${displayAngle})` },\n          { function: 'tan', value: Math.sin(radAngle) / Math.cos(radAngle), formula: `sin/cos(${displayAngle})` }\n        );\n        break;\n      case 'cos':\n        conversions.push(\n          { function: 'cos', value: Math.cos(radAngle), formula: `cos(${displayAngle})` },\n          { function: 'sin', value: Math.sin(Math.PI/2 - radAngle), formula: `sin(${complementAngle})` },\n          { function: 'sec', value: 1 / Math.cos(radAngle), formula: `1/cos(${displayAngle})` },\n          { function: 'tan', value: Math.cos(radAngle) / Math.sin(radAngle), formula: `cos/sin(${displayAngle})` }\n        );\n        break;\n      case 'tan':\n        conversions.push(\n          { function: 'tan', value: Math.tan(radAngle), formula: `tan(${displayAngle})` },\n          { function: 'sin', value: Math.sin(radAngle), formula: `sin(${displayAngle})` },\n          { function: 'cos', value: Math.cos(radAngle), formula: `cos(${displayAngle})` },\n          { function: 'cot', value: 1 / Math.tan(radAngle), formula: `1/tan(${displayAngle})` }\n        );\n        break;\n      case 'csc':\n        conversions.push(\n          { function: 'csc', value: 1 / Math.sin(radAngle), formula: `csc(${displayAngle})` },\n          { function: 'sin', value: Math.sin(radAngle), formula: `sin(${displayAngle})` },\n          { function: 'sec', value: 1 / Math.cos(Math.PI/2 - radAngle), formula: `sec(${complementAngle})` },\n          { function: 'cot', value: Math.cos(radAngle) / Math.sin(radAngle), formula: `cot(${displayAngle})` }\n        );\n        break;\n      case 'sec':\n        conversions.push(\n          { function: 'sec', value: 1 / Math.cos(radAngle), formula: `sec(${displayAngle})` },\n          { function: 'cos', value: Math.cos(radAngle), formula: `cos(${displayAngle})` },\n          { function: 'csc', value: 1 / Math.sin(Math.PI/2 - radAngle), formula: `csc(${complementAngle})` },\n          { function: 'tan', value: Math.tan(radAngle), formula: `tan(${displayAngle})` }\n        );\n        break;\n      case 'cot':\n        conversions.push(\n          { function: 'cot', value: 1 / Math.tan(radAngle), formula: `cot(${displayAngle})` },\n          { function: 'tan', value: Math.tan(radAngle), formula: `tan(${displayAngle})` },\n          { function: 'cos', value: Math.cos(radAngle), formula: `cos(${displayAngle})` },\n          { function: 'sin', value: Math.sin(radAngle), formula: `sin(${displayAngle})` }\n        );\n        break;\n    }\n\n    return conversions;\n  };\n\n  const trigFunctions: TrigFunction[] = ['sin', 'cos', 'tan', 'csc', 'sec', 'cot'];\n  const conversions = getConversions();\n\n  if (!mounted) return null;\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900 transition-colors duration-300\">\n      {/* Header */}\n      <header className=\"bg-white dark:bg-gray-800 shadow-sm border-b border-gray-200 dark:border-gray-700 transition-colors duration-300\">\n        <div className=\"max-w-7xl mx-auto px-6 py-6 flex items-center justify-between\">\n          <div>\n            <h1 className=\"text-3xl font-bold text-gray-900 dark:text-white\">Trigonometric Calculator</h1>\n            <p className=\"text-gray-600 dark:text-gray-400 mt-1\">Convert between sin, cos, tan, csc, sec, and cot</p>\n          </div>\n          <button\n            onClick={toggleTheme}\n            className=\"p-3 rounded-xl bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 transition-all duration-300 cursor-pointer\"\n            aria-label=\"Toggle theme\"\n          >\n            {theme === 'light' ? (\n              <svg className=\"w-6 h-6 text-gray-700\" fill=\"none\" viewBox=\"0 0 24 24\" stroke=\"currentColor\">\n                <path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} d=\"M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z\" />\n              </svg>\n            ) : (\n              <svg className=\"w-6 h-6 text-yellow-400\" fill=\"none\" viewBox=\"0 0 24 24\" stroke=\"currentColor\">\n                <path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} d=\"M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z\" />\n              </svg>\n            )}\n          </button>\n        </div>\n      </header>\n\n      {/* Main Content */}\n      <main className=\"max-w-7xl mx-auto px-6 py-12\">\n        {/* Calculator Card */}\n        <div className=\"bg-white dark:bg-gray-800 rounded-2xl shadow-xl p-10 border border-gray-200 dark:border-gray-700 transition-colors duration-300\">\n          {/* Input Section */}\n          <div className=\"space-y-6\">\n            {/* Angle Input */}\n            <div>\n              <label className=\"block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-3\">Enter Angle</label>\n              <div className=\"flex gap-4\">\n                <input\n                  type=\"number\"\n                  value={angle}\n                  onChange={(e: React.ChangeEvent<HTMLInputElement>) => setAngle(e.target.value)}\n                  className=\"flex-1 px-6 py-4 text-lg border-2 border-gray-300 dark:border-gray-600 rounded-xl focus:ring-4 focus:ring-blue-500/20 focus:border-blue-500 dark:bg-gray-700 dark:text-white transition-all duration-300\"\n                  placeholder=\"Enter angle value\"\n                />\n                <button\n                  onClick={() => setAngleMode(angleMode === 'deg' ? 'rad' : 'deg')}\n                  className=\"px-8 py-4 bg-blue-500 hover:bg-blue-600 text-white font-semibold rounded-xl transition-all duration-300 cursor-pointer shadow-lg hover:shadow-xl\"\n                >\n                  {angleMode === 'deg' ? 'Degrees' : 'Radians'}\n                </button>\n              </div>\n            </div>\n\n            {/* Function Selection */}\n            <div>\n              <label className=\"block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-3\">Select Function</label>\n              <div className=\"grid grid-cols-2 sm:grid-cols-3 md:grid-cols-6 gap-3\">\n                {trigFunctions.map((func: TrigFunction) => (\n                  <button\n                    key={func}\n                    onClick={() => setSelectedFunction(func)}\n                    className={`px-6 py-4 rounded-xl font-semibold text-lg transition-all duration-300 cursor-pointer ${\n                      selectedFunction === func\n                        ? 'bg-blue-500 text-white shadow-lg scale-105'\n                        : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600'\n                    }`}\n                  >\n                    {func}\n                  </button>\n                ))}\n              </div>\n            </div>\n          </div>\n\n          {/* Results Section */}\n          <div className=\"mt-12\">\n            <h2 className=\"text-2xl font-bold text-gray-900 dark:text-white mb-6\">Results & Conversions</h2>\n            <div className=\"grid grid-cols-1 md:grid-cols-2 gap-6\">\n              {conversions.map((result: ConversionResult, index: number) => (\n                <div\n                  key={index}\n                  className=\"bg-gradient-to-br from-blue-50 to-purple-50 dark:from-gray-700 dark:to-gray-600 rounded-xl p-8 border-2 border-blue-200 dark:border-gray-500 transition-all duration-300 hover:shadow-lg\"\n                >\n                  <div className=\"flex items-center justify-between mb-4\">\n                    <span className=\"text-2xl font-bold text-blue-600 dark:text-blue-400\">{result.function}</span>\n                    {index === 0 && (\n                      <span className=\"px-3 py-1 bg-blue-500 text-white text-xs font-semibold rounded-full\">PRIMARY</span>\n                    )}\n                  </div>\n                  <div className=\"text-3xl font-bold text-gray-900 dark:text-white mb-2\">\n                    {isFinite(result.value) ? result.value.toFixed(6) : 'Undefined'}\n                  </div>\n                  <div className=\"text-sm text-gray-600 dark:text-gray-400 font-mono\">{result.formula}</div>\n                </div>\n              ))}\n            </div>\n          </div>\n\n          {/* Quick Reference */}\n          <div className=\"mt-12 p-8 bg-gradient-to-r from-blue-50 to-purple-50 dark:from-gray-700 dark:to-gray-600 rounded-xl border border-blue-200 dark:border-gray-500\">\n            <h3 className=\"text-xl font-bold text-gray-900 dark:text-white mb-4\">Quick Reference</h3>\n            <div className=\"grid grid-cols-1 md:grid-cols-2 gap-4 text-sm\">\n              <div className=\"space-y-2\">\n                <p className=\"text-gray-700 dark:text-gray-300\"><span className=\"font-semibold\">sin(θ) =</span> cos(90° - θ)</p>\n                <p className=\"text-gray-700 dark:text-gray-300\"><span className=\"font-semibold\">cos(θ) =</span> sin(90° - θ)</p>\n                <p className=\"text-gray-700 dark:text-gray-300\"><span className=\"font-semibold\">tan(θ) =</span> sin(θ) / cos(θ)</p>\n              </div>\n              <div className=\"space-y-2\">\n                <p className=\"text-gray-700 dark:text-gray-300\"><span className=\"font-semibold\">csc(θ) =</span> 1 / sin(θ)</p>\n                <p className=\"text-gray-700 dark:text-gray-300\"><span className=\"font-semibold\">sec(θ) =</span> 1 / cos(θ)</p>\n                <p className=\"text-gray-700 dark:text-gray-300\"><span className=\"font-semibold\">cot(θ) =</span> 1 / tan(θ)</p>\n              </div>\n            </div>\n          </div>\n        </div>\n\n        {/* Common Angles Reference */}\n        <div className=\"mt-8 bg-white dark:bg-gray-800 rounded-2xl shadow-xl p-10 border border-gray-200 dark:border-gray-700 transition-colors duration-300\">\n          <h2 className=\"text-2xl font-bold text-gray-900 dark:text-white mb-6\">Common Angles</h2>\n          <div className=\"overflow-x-auto\">\n            <table className=\"w-full\">\n              <thead>\n                <tr className=\"border-b-2 border-gray-200 dark:border-gray-700\">\n                  <th className=\"px-4 py-3 text-left text-sm font-semibold text-gray-700 dark:text-gray-300\">Angle</th>\n                  <th className=\"px-4 py-3 text-left text-sm font-semibold text-gray-700 dark:text-gray-300\">sin</th>\n                  <th className=\"px-4 py-3 text-left text-sm font-semibold text-gray-700 dark:text-gray-300\">cos</th>\n                  <th className=\"px-4 py-3 text-left text-sm font-semibold text-gray-700 dark:text-gray-300\">tan</th>\n                </tr>\n              </thead>\n              <tbody className=\"divide-y divide-gray-200 dark:divide-gray-700\">\n                {[0, 30, 45, 60, 90].map((deg: number) => {\n                  const rad = toRadians(deg);\n                  return (\n                    <tr key={deg} className=\"hover:bg-gray-50 dark:hover:bg-gray-700 cursor-pointer transition-colors duration-200\" onClick={() => setAngle(deg.toString())}>\n                      <td className=\"px-4 py-3 text-gray-700 dark:text-gray-300 font-medium\">{deg}°</td>\n                      <td className=\"px-4 py-3 text-gray-600 dark:text-gray-400\">{Math.sin(rad).toFixed(4)}</td>\n                      <td className=\"px-4 py-3 text-gray-600 dark:text-gray-400\">{Math.cos(rad).toFixed(4)}</td>\n                      <td className=\"px-4 py-3 text-gray-600 dark:text-gray-400\">{isFinite(Math.tan(rad)) ? Math.tan(rad).toFixed(4) : '∞'}</td>\n                    </tr>\n                  );\n                })}\n              </tbody>\n            </table>\n          </div>\n        </div>\n      </main>\n\n      <SynoppyBadge />\n    </div>\n  );\n}"}]